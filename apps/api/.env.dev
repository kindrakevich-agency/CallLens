
###> symfony/framework-bundle ###
# Non-secret placeholder. The real APP_SECRET is injected from the environment
# (root .env in dev — gitignored; Symfony secrets vault in prod). Never commit a real one.
APP_SECRET=dev_placeholder_override_via_environment
###< symfony/framework-bundle ###
